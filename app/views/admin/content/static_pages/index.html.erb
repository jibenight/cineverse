<% content_for(:title) { "Pages statiques" } %>
<% content_for(:page_title) { "Pages statiques" } %>

<div class="mb-6">
  <span class="text-sm text-cv-muted"><%= @pages.size %> pages</span>
</div>

<div class="bg-cv-surface rounded-xl border border-cv-border overflow-hidden">
  <table class="w-full">
    <thead>
      <tr class="border-b border-cv-border text-left">
        <th class="px-4 py-3 text-xs font-medium text-cv-muted uppercase tracking-wider">Titre</th>
        <th class="px-4 py-3 text-xs font-medium text-cv-muted uppercase tracking-wider">Slug</th>
        <th class="px-4 py-3 text-xs font-medium text-cv-muted uppercase tracking-wider">Derniere MAJ</th>
        <th class="px-4 py-3 text-xs font-medium text-cv-muted uppercase tracking-wider">Actions</th>
      </tr>
    </thead>
    <tbody class="divide-y divide-neutral-800">
      <% @pages.each do |page| %>
        <tr class="hover:bg-cv-hover/50 transition">
          <td class="px-4 py-3 text-sm font-medium"><%= page.title %></td>
          <td class="px-4 py-3 text-sm text-cv-muted"><%= page.slug %></td>
          <td class="px-4 py-3 text-sm text-cv-muted"><%= l(page.updated_at, format: :short) %></td>
          <td class="px-4 py-3">
            <%= link_to "Modifier", edit_admin_content_static_page_path(page), class: "text-xs text-amber-500 hover:text-amber-400" %>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>
