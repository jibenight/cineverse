<% content_for(:title) { "Modifier la campagne" } %>
<% content_for(:page_title) { "Modifier : #{@campaign.subject}" } %>

<div class="mb-6">
  <%= link_to "← Retour a la campagne", admin_newsletter_campaign_path(@campaign), class: "text-sm text-cv-muted hover:text-amber-500" %>
</div>

<div class="bg-cv-surface rounded-xl p-6 border border-cv-border">
  <%= render "form", campaign: @campaign %>
</div>
