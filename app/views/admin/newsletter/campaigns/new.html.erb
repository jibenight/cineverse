<% content_for(:title) { "Nouvelle campagne" } %>
<% content_for(:page_title) { "Nouvelle campagne" } %>

<div class="mb-6">
  <%= link_to "← Retour aux campagnes", admin_newsletter_campaigns_path, class: "text-sm text-cv-muted hover:text-amber-500" %>
</div>

<div class="bg-cv-surface rounded-xl p-6 border border-cv-border">
  <%= render "form", campaign: @campaign %>
</div>
